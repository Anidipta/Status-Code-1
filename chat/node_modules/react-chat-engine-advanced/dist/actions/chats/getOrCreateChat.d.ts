import { ChatObject, UserAuthHeaders } from '../../interfaces';
interface NewChat {
    is_direct_chat?: boolean;
    usernames: string[];
}
export declare const getOrCreateChat: (host: string | undefined, headers: UserAuthHeaders, data: NewChat, callback: (chat: ChatObject) => void) => void;
export {};
